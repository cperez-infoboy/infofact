fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20
python-jose[cryptography]>=3.3
bcrypt>=4.0
python-multipart>=0.0.9
httpx>=0.27
ddgs>=9.0
# Pinado a la version del contenedor de produccion: langchain 1.3.15
# introduce el marcado lc_internal_call de llamadas internas de middleware,
# que el relay SSE de chat.py filtra (sesion 48). Versiones menores por
# debajo no marcan esas llamadas y el filtro no dispara.
deepagents==0.7.6
# AgentMiddleware / ModelRequest (backend/agents/size_guard.py): hoy entra
# transitiva por deepagents, pero la usamos directo: fijarla explicita.
langchain==1.3.15
langchain-openai>=0.2
langgraph-checkpoint-sqlite>=2.0
docling>=2.0
# transformers v5 removes RTDetrImageProcessor, which docling-ibm-models still
# imports at runtime; pin <5 or the layout model crashes on first convert().
transformers>=4.40,<5
tiktoken>=0.8
rapidfuzz>=3.0
# Local embeddings for requirements consolidation (dedup + contradictions).
# Host-side to keep client docs confidential (no embeddings API). Multilingual
# model for Spanish docs. Torch already a dependency (docling).
sentence-transformers>=3.0
# NOTE: torch + torchvision are NOT listed here because requirements.txt cannot
# express a separate package index. They must be installed FIRST from the CPU
# index so the pair aligns, or torchvision::nms is missing at runtime:
#   uv pip install --index-url https://download.pytorch.org/whl/cpu torch torchvision
# In Docker this is a separate RUN before `pip install -r requirements.txt`.
